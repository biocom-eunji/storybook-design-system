import React from 'react';
import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Semantics/Border Radius" />

export const RadiusRow = ({ token, alias, px, role }) => {
  const [copied, setCopied] = React.useState(false);
  const handleCopy = () => {
    navigator.clipboard.writeText(token).then(() => {
      setCopied(true);
      setTimeout(() => setCopied(false), 1500);
    });
  };
  return (
    <tr className="token-row" onClick={handleCopy} style={{ cursor: 'pointer' }}>
      <td className="token-name-cell">
        <code>{alias || token}</code>
        {copied && <span className="copied-badge">복사됨!</span>}
      </td>
      <td>
        <div style={{
          width: 28,
          height: 28,
          backgroundColor: '#22C3BC',
          borderTopRightRadius: Math.min(px, 14),
        }} />
      </td>
      <td className="value-cell">{px === 9999 ? '9999px' : `${px}px`}</td>
      <td className="role-cell">{role}</td>
    </tr>
  );
};

<style>
  {`
    .radius-page {
      max-width: 900px;
      margin: 0 auto;
      font-family: 'Pretendard Variable', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      color: #37383C;
    }
    .radius-page h2.page-title {
      font-size: 28px;
      font-weight: 700;
      color: #171719;
      margin: 0 0 8px;
      padding-bottom: 12px;
      border-bottom: 2px solid #22C3BC;
    }
    .radius-page > p.page-desc {
      font-size: 14px;
      color: #70737C;
      margin: 0 0 24px;
      line-height: 1.7;
    }
    .radius-table {
      width: 100%;
      border-collapse: collapse;
      font-size: 13px;
    }
    .radius-table thead th {
      text-align: left;
      padding: 10px 14px;
      font-weight: 600;
      color: #46474C;
      font-size: 12px;
      background: #F7F7F8;
      border-bottom: 2px solid #E1E2E4;
    }
    .radius-table .token-row td {
      padding: 10px 14px;
      border-bottom: 1px solid #F4F4F5;
      vertical-align: middle;
    }
    .radius-table .token-row:hover td {
      background: #FBFEFE;
    }
    .radius-table .token-name-cell {
      position: relative;
    }
    .radius-table .token-name-cell code {
      font-family: 'SF Mono', 'Fira Code', monospace;
      font-size: 12px;
      color: #22C3BC;
      background: #E9FBF5;
      padding: 2px 8px;
      border-radius: 4px;
      white-space: nowrap;
    }
    .radius-table .copied-badge {
      position: absolute;
      top: -4px;
      right: 0;
      font-size: 10px;
      font-weight: 600;
      color: #22C3BC;
      background: #E9FBF5;
      padding: 2px 6px;
      border-radius: 4px;
    }
    .radius-table .value-cell {
      font-family: 'SF Mono', 'Fira Code', monospace;
      font-size: 12px;
      font-weight: 600;
      color: #37383C;
    }
    .radius-table .role-cell {
      font-size: 12px;
      color: #70737C;
    }
    .section-divider {
      border: none;
      border-top: 1px solid #E1E2E4;
      margin: 36px 0;
    }
    .usage-card {
      background: linear-gradient(135deg, #FBFEFE 0%, #E9FBFA 100%);
      border: 1px solid #D4F7F6;
      border-radius: 12px;
      padding: 20px 24px;
      margin: 16px 0 0;
      overflow-x: auto;
    }
    .usage-card pre {
      margin: 0;
      font-family: 'SF Mono', 'Fira Code', monospace;
      font-size: 13px;
      line-height: 1.7;
      color: #37383C;
    }
    .usage-card .comment { color: #70737C; }
    .usage-card .token { color: #22C3BC; }
  `}
</style>

<div className="radius-page">

<h2 className="page-title">Border Radius</h2>
<p className="page-desc">
  Figma Semantics/borderRadius 변수와 1:1 매핑되는 모서리 반경 토큰입니다. 토큰을 클릭하면 코드가 클립보드에 복사됩니다.
</p>

<table className="radius-table">
  <thead>
    <tr>
      <th style={{ width: '30%' }}>Token Name</th>
      <th style={{ width: '10%' }}>Preview</th>
      <th style={{ width: '10%' }}>Value</th>
      <th>역할 (Role)</th>
    </tr>
  </thead>
  <tbody>
    <RadiusRow token="borderRadius/xsmall" alias="radius.xsmall" px={4}    role="소형 태그, 코드 블록, Chip XSmall" />
    <RadiusRow token="borderRadius/small"  alias="radius.small"  px={8}    role="Small 버튼, Small/Medium 칩, Checkbox" />
    <RadiusRow token="borderRadius/medium" alias="radius.medium" px={12}   role="Medium/Large 버튼, 인풋, 카드, Modal" />
    <RadiusRow token="borderRadius/large"  alias="radius.large"  px={16}   role="큰 카드, 섹션 컨테이너" />
    <RadiusRow token="borderRadius/xlarge" alias="radius.xlarge" px={24}   role="BottomSheet 상단, 대형 카드" />
    <RadiusRow token="borderRadius/full"   alias="radius.full"   px={9999} role="원형 (배지, 아바타, Switch, 토글)" />
  </tbody>
</table>

<hr className="section-divider" />

### Usage

<p>컴포넌트에서 border radius 토큰을 사용하는 예시입니다.</p>

<div className="usage-card">
  <pre>
    <span className="comment">// theme.ts에서 import</span>{'\n'}
    import {'{ '}<span className="token">radius</span>{' }'} from '../tokens/theme';{'\n'}
    {'\n'}
    <span className="comment">// 기본 사용</span>{'\n'}
    {'<View style={{ borderRadius: '}<span className="token">radius.medium</span>{' }}>'}<span className="comment">{'  '}// 12px</span>{'\n'}
    {'\n'}
    <span className="comment">// 원형</span>{'\n'}
    {'<View style={{ borderRadius: '}<span className="token">radius.full</span>{' }}>'}<span className="comment">{'  '}// 9999px</span>{'\n'}
  </pre>
</div>

</div>
